import {
  productStyle,
  relatedSkus,
} from './handlers'

const productResolver = {
  StoreProduct: {
    /**
     * @param ProductStoreRoot
     * @returns
     */
    productStyle,
    relatedSkus,
  },
}

export default productResolver
